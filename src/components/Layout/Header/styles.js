import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  header: {
    backgroundColor: '#333333',
    height: '56px',
    color: 'white',
    fontSize: 28,
    letterSpacing: 3,
    marginBottom: 30,
    borderRadius: 4,
  },
});
