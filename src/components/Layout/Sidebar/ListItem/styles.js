import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  listItems: {
    padding: '10px',
  },
  linkWidth: {
    width: '100%',
  },
  buttonWidth: {
    width: '100%',
    textAlign: 'left',
  }
});
