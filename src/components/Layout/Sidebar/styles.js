import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  container: {
    border: '1px solid #DCDCDC ',
    borderRadius: '4px',
    marginRight: '2em',
  },
  link: {
    textDecoration: 'none',
    color: 'inherit',
  },
});
